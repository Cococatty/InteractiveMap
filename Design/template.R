library(shiny)
library(datasets)
library(ggplot2)


# Define UI for iris application
ui <- pageWithSidebar(
  
)


server <- function(input, output, session) {
  
  
}


shinyApp(ui = ui, server = server)